define(["chai", "studentSystem"], function (chai, studentSystem) {
	var should = chai.should();
	var expect = chai.expect;
	var assert = chai.assert;

	describe("Student System Test",function(){
		it("should return 1", function () {
			expect(1).to.equal(1);

		});
	});
});
